# Piano Sub-Page Navigation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the hidden hamburger menu on Portfolio, Experience, and About pages with a visible, piano-themed navigation bar fused with each page title.

**Architecture:** A fixed top header (`<header class="page-header">`) contains the page title (left) and nav links (right) on a single row, with a generated piano-key accent line below. Pure CSS + vanilla JS. No new dependencies.

**Tech Stack:** HTML, CSS, vanilla JavaScript (ES5-compatible for consistency with existing codebase).

---

## File Structure

| File | Responsibility |
|------|---------------|
| `css/styles.css` | All `.page-header`, `.page-nav`, `.piano-accent` styles, responsive breakpoints, z-index layering |
| `js/nav.js` | Generate piano key segments for accent line, handle hover/active states, window resize |
| `portfolio.html` | Add header block, remove hamburger menu HTML + `initMenu()` call |
| `experience.html` | Same as portfolio, adapted for `.experience-ariel` structure |
| `about.html` | Same as portfolio |

---

## Task 1: Add Navigation CSS

**Files:**
- Modify: `css/styles.css` (append to end of file)

**Context:** All three sub-pages use a warm ivory background (`#F2ECE2`). The `.experience-ariel` page uses accent `#9E6347` instead of `#D4A574`. The transition overlay has `z-index: 100`; the header must stay below it.

- [ ] **Step 1: Append navigation styles**

Add the following to the end of `css/styles.css`:

```css
/* ─── Page Header Navigation ────────────────── */
.page-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 90;
  background: #F2ECE2;
}

/* Push page content down to avoid being hidden under fixed header */
body:has(.page-header) .page {
  padding-top: 64px;
}

.page-header-inner {
  max-width: 1200px;
  margin: 0 auto;
  padding: 1.25rem 2rem 0.5rem;
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 1rem;
}

/* Title inside header: preserve existing .page-title animation classes */
.page-header .page-title {
  margin: 0;
  padding-top: 0;
  font-size: clamp(1.5rem, 3vw, 2rem);
}

.page-nav {
  display: flex;
  align-items: center;
  gap: 1.75rem;
}

.page-nav a {
  font-family: var(--font-sans);
  font-size: 0.8125rem;
  color: #8B7E73;
  text-decoration: none;
  transition: color 0.2s ease;
  white-space: nowrap;
}

.page-nav a:hover {
  color: #D4A574;
}

.page-nav a[aria-current="page"] {
  font-weight: 600;
  color: #1C1916;
  cursor: default;
}

/* Piano accent line */
.piano-accent {
  height: 2px;
  display: flex;
  justify-content: flex-end;
  gap: 1px;
  overflow: hidden;
  padding: 0 2rem;
}

.piano-accent .key-white {
  height: 100%;
  background: #D4C4B0;
  border-radius: 0 0 2px 2px;
  flex-shrink: 0;
}

.piano-accent .key-black {
  height: 65%;
  margin-top: -1px;
  background: #6B5E53;
  border-radius: 0 0 2px 2px;
  flex-shrink: 0;
}

/* Hover: entire accent line brightens */
.piano-accent.is-hovered .key-white,
.piano-accent.is-hovered .key-black {
  background: #D4A574;
}

/* Experience page accent override */
.experience-ariel .page-nav a:hover {
  color: #9E6347;
}

.experience-ariel .piano-accent.is-hovered .key-white,
.experience-ariel .piano-accent.is-hovered .key-black {
  background: #9E6347;
}

/* Mobile */
@media (max-width: 768px) {
  .page-header-inner {
    flex-direction: column;
    align-items: flex-start;
    padding: 0.875rem 1rem 0.375rem;
    gap: 0.5rem;
  }

  .page-header .page-title {
    font-size: 1.125rem;
  }

  .page-nav {
    gap: 1rem;
  }

  .page-nav a {
    font-size: 0.6875rem;
  }

  .piano-accent {
    justify-content: flex-start;
    padding: 0 1rem;
  }

  body:has(.page-header) .page {
    padding-top: 88px;
  }
}
```

- [ ] **Step 2: Verify CSS syntax**

Run: `npx stylelint css/styles.css --config "{\"rules\":{}}" 2>/dev/null || echo "stylelint not available, skipping"`

Expected: No critical parse errors (browser will also validate on load).

- [ ] **Step 3: Commit**

```bash
git add css/styles.css
git commit -m "feat: add page-header navigation styles with piano accent"
```

---

## Task 2: Create nav.js

**Files:**
- Create: `js/nav.js`

**Context:** The piano accent line needs enough key divs to span the viewport. The pattern is one octave: W B W B W W B W (8 keys). On resize, keys regenerate. The active page is determined from `location.pathname`.

- [ ] **Step 1: Write nav.js**

Create `js/nav.js`:

```javascript
window.PianoApp = window.PianoApp || {};

window.PianoApp.initNav = function () {
  var nav = document.querySelector('.page-nav');
  var accent = document.querySelector('.piano-accent');
  if (!nav || !accent) return;

  // Mark active page based on current URL
  var currentPath = window.location.pathname.split('/').pop() || 'index.html';
  nav.querySelectorAll('a').forEach(function (link) {
    var href = link.getAttribute('href');
    if (href === currentPath) {
      link.setAttribute('aria-current', 'page');
    }
  });

  // Generate piano accent keys
  function generateKeys() {
    accent.innerHTML = '';
    var whiteW = window.innerWidth <= 768 ? 10 : 16;
    var blackW = window.innerWidth <= 768 ? 7 : 10;
    var gap = 1;

    // One octave pattern: W B W B W W B W
    var pattern = [
      { type: 'white', w: whiteW },
      { type: 'black', w: blackW },
      { type: 'white', w: whiteW },
      { type: 'black', w: blackW },
      { type: 'white', w: whiteW },
      { type: 'white', w: whiteW },
      { type: 'black', w: blackW },
      { type: 'white', w: whiteW }
    ];

    var patternWidth = pattern.reduce(function (sum, k) {
      return sum + k.w + gap;
    }, 0) - gap;

    var count = Math.ceil(window.innerWidth / patternWidth) + 2;

    for (var i = 0; i < count; i++) {
      pattern.forEach(function (key) {
        var el = document.createElement('div');
        el.className = key.type === 'white' ? 'key-white' : 'key-black';
        el.style.width = key.w + 'px';
        accent.appendChild(el);
      });
    }
  }

  generateKeys();

  // Regenerate on resize (debounced)
  var resizeTimer;
  window.addEventListener('resize', function () {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(generateKeys, 150);
  });

  // Hover effect: brighten entire accent line
  nav.querySelectorAll('a').forEach(function (link) {
    link.addEventListener('mouseenter', function () {
      accent.classList.add('is-hovered');
    });
    link.addEventListener('mouseleave', function () {
      accent.classList.remove('is-hovered');
    });
  });
};
```

- [ ] **Step 2: Verify file was created**

Run: `ls -la js/nav.js`

Expected: File exists with non-zero size.

- [ ] **Step 3: Commit**

```bash
git add js/nav.js
git commit -m "feat: add nav.js for piano accent line and active state"
```

---

## Task 3: Update Portfolio HTML

**Files:**
- Modify: `portfolio.html`

**Context:** The current structure has `.menu-btn` (line 37), `.menu-overlay` (line 46), and `<h1 class="page-title">` inside `.page-content > .container` (line 64). All three must be moved/replaced.

- [ ] **Step 1: Remove hamburger menu HTML**

Delete lines 37–58 (from `<!-- Menu Button -->` through closing `</div>` of menu-overlay).

- [ ] **Step 2: Add header block before `.page`**

Insert immediately before `<!-- Portfolio Page -->`:

```html
  <!-- Page Header -->
  <header class="page-header">
    <div class="page-header-inner">
      <h1 class="page-title animate-fade-in-up delay-1">Use of useless.</h1>
      <nav class="page-nav" aria-label="Primary">
        <a href="index.html">Home</a>
        <a href="portfolio.html" aria-current="page">Portfolio</a>
        <a href="experience.html">Experience</a>
        <a href="about.html">About</a>
      </nav>
    </div>
    <div class="piano-accent" aria-hidden="true"></div>
  </header>
```

- [ ] **Step 3: Remove title from page-content and simplify container**

The original code inside `.page-content` is:

```html
    <div class="page-content">
      <div class="container">
        <h1 class="page-title animate-fade-in-up delay-1">Use of useless.</h1>
      </div>

      <!-- Portfolio Showcase -->
      <div id="portfolio-showcase" class="portfolio-showcase"></div>

      <p class="portfolio-epilogue animate-fade-in-up delay-3">无用之用</p>
    </div>
```

Replace with:

```html
    <div class="page-content">
      <!-- Portfolio Showcase -->
      <div id="portfolio-showcase" class="portfolio-showcase"></div>

      <p class="portfolio-epilogue animate-fade-in-up delay-3">无用之用</p>
    </div>
```

- [ ] **Step 4: Replace menu.js with nav.js and remove initMenu()**

In the script block at the bottom, change:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initMenu();
      window.PianoApp.initPortfolio();
```

to:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initNav();
      window.PianoApp.initPortfolio();
```

And change the script tag from:
```html
  <script src="js/menu.js"></script>
```
to:
```html
  <script src="js/nav.js"></script>
```

- [ ] **Step 5: Verify the file structure**

Open `portfolio.html` and confirm:
1. No `.menu-btn` or `.menu-overlay` elements remain
2. `<header class="page-header">` appears before `<div class="page page-warm">`
3. The title inside `.page-content` has been removed
4. `js/nav.js` is loaded, `js/menu.js` is not
5. `initMenu()` is not called, `initNav()` is

- [ ] **Step 6: Commit**

```bash
git add portfolio.html
git commit -m "feat: add page-header nav to portfolio, remove hamburger menu"
```

---

## Task 4: Update Experience HTML

**Files:**
- Modify: `experience.html`

**Context:** Experience page differs from Portfolio: it uses `.experience-ariel` instead of `.page-warm`, has no `.page-content` wrapper, and the title has no animation classes.

- [ ] **Step 1: Remove hamburger menu HTML**

Delete lines 18–40 (from `<!-- Menu Button -->` through closing `</div>` of menu-overlay).

- [ ] **Step 2: Add header block before `.page`**

Insert immediately before `<!-- Experience Page -->`:

```html
  <!-- Page Header -->
  <header class="page-header">
    <div class="page-header-inner">
      <h1 class="page-title">Exploration.</h1>
      <nav class="page-nav" aria-label="Primary">
        <a href="index.html">Home</a>
        <a href="portfolio.html">Portfolio</a>
        <a href="experience.html" aria-current="page">Experience</a>
        <a href="about.html">About</a>
      </nav>
    </div>
    <div class="piano-accent" aria-hidden="true"></div>
  </header>
```

- [ ] **Step 3: Remove title from inside `.container`**

The original code inside `.experience-ariel > .container` starts with:

```html
      <h1 class="page-title">Exploration.</h1>
```

Remove this line.

- [ ] **Step 4: Replace menu.js with nav.js and remove initMenu()**

In the script block at the bottom, change:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initMenu();
```

to:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initNav();
```

And change the script tag from:
```html
  <script src="js/menu.js"></script>
```
to:
```html
  <script src="js/nav.js"></script>
```

- [ ] **Step 5: Verify the file structure**

Open `experience.html` and confirm the same 5 points as portfolio.

- [ ] **Step 6: Commit**

```bash
git add experience.html
git commit -m "feat: add page-header nav to experience, remove hamburger menu"
```

---

## Task 5: Update About HTML

**Files:**
- Modify: `about.html`

**Context:** About page structure is nearly identical to Portfolio: `.page-warm`, `.page-content > .container > h1.page-title` with animation classes.

- [ ] **Step 1: Remove hamburger menu HTML**

Delete lines 17–39 (from `<!-- Menu Button -->` through closing `</div>` of menu-overlay).

- [ ] **Step 2: Add header block before `.page`**

Insert immediately before `<!-- About Page -->`:

```html
  <!-- Page Header -->
  <header class="page-header">
    <div class="page-header-inner">
      <h1 class="page-title animate-fade-in-up delay-1">About.</h1>
      <nav class="page-nav" aria-label="Primary">
        <a href="index.html">Home</a>
        <a href="portfolio.html">Portfolio</a>
        <a href="experience.html">Experience</a>
        <a href="about.html" aria-current="page">About</a>
      </nav>
    </div>
    <div class="piano-accent" aria-hidden="true"></div>
  </header>
```

- [ ] **Step 3: Remove title from page-content and simplify container**

The original code inside `.page-content` is:

```html
    <div class="page-content">
      <div class="container">
        <h1 class="page-title animate-fade-in-up delay-1">About.</h1>
      </div>
      <div id="about-container" class="about-container about-stagger">
```

Replace with:

```html
    <div class="page-content">
      <div id="about-container" class="about-container about-stagger">
```

- [ ] **Step 4: Replace menu.js with nav.js and remove initMenu()**

In the script block at the bottom, change:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initMenu();
      window.PianoApp.initAbout();
```

to:

```javascript
      window.PianoApp.initTransitions();
      window.PianoApp.initNav();
      window.PianoApp.initAbout();
```

And change the script tag from:
```html
  <script src="js/menu.js"></script>
```
to:
```html
  <script src="js/nav.js"></script>
```

- [ ] **Step 5: Verify the file structure**

Open `about.html` and confirm the same 5 points as portfolio.

- [ ] **Step 6: Commit**

```bash
git add about.html
git commit -m "feat: add page-header nav to about, remove hamburger menu"
```

---

## Task 6: Verify All Pages

**Files:**
- Test: `portfolio.html`, `experience.html`, `about.html` in browser

- [ ] **Step 1: Start a local server**

Run: `python3 -m http.server 8080 &`

- [ ] **Step 2: Check Portfolio page**

Open `http://localhost:8080/portfolio.html`

Verify:
- [ ] Header is fixed at top, title "Use of useless." on left, nav links on right
- [ ] Piano accent line visible below header
- [ ] "Portfolio" link is bold/dark (active state)
- [ ] Hovering any nav link changes link color and accent line to warm gold
- [ ] Scrolling down keeps header visible
- [ ] No hamburger button visible
- [ ] Page transitions still work when clicking nav links

- [ ] **Step 3: Check Experience page**

Open `http://localhost:8080/experience.html`

Verify:
- [ ] Same layout, title "Exploration."
- [ ] "Experience" link is active
- [ ] Hover accent color is `#9E6347` (copper) instead of `#D4A574`
- [ ] Map and timeline content render correctly below header

- [ ] **Step 4: Check About page**

Open `http://localhost:8080/about.html`

Verify:
- [ ] Same layout, title "About."
- [ ] "About" link is active
- [ ] Avatar, bio, skills render correctly below header

- [ ] **Step 5: Check mobile responsive**

Use browser dev tools, set viewport to 375px width. On any sub-page:
- [ ] Title stacks above nav links
- [ ] Nav links are left-aligned, smaller font
- [ ] Piano accent line is left-aligned
- [ ] Header still fixed while scrolling

- [ ] **Step 6: Check homepage is untouched**

Open `http://localhost:8080/index.html`
- [ ] No navigation header appears
- [ ] Piano keys still work for navigation
- [ ] No errors in console

- [ ] **Step 7: Final commit**

```bash
git commit --allow-empty -m "feat: piano-themed navigation complete on all sub-pages"
```

---

## Plan Self-Review

### Spec Coverage Check

| Spec Requirement | Implementation Task |
|-----------------|---------------------|
| Fused header: title left, nav right | Task 1 (CSS), Tasks 3–5 (HTML) |
| Piano keyboard accent line | Task 1 (CSS), Task 2 (JS generation) |
| Fixed position while scrolling | Task 1 (`.page-header { position: fixed }`) |
| Only on 3 sub-pages | Tasks 3–5 (HTML changes) |
| Index page unchanged | Task 6 Step 6 (verification) |
| Warm theme only | Task 1 (colors hardcoded to warm palette) |
| Active page highlighted | Task 1 (CSS `[aria-current]`), Task 2 (JS sets attr) |
| Hover: link + accent line sync | Task 1 (CSS), Task 2 (JS `.is-hovered` class) |
| Mobile: vertical stack | Task 1 (`@media (max-width: 768px)`) |
| Page transitions integration | Task 6 Step 2 (verification), transitions.js already intercepts all `a[href]` |
| Hamburger menu hidden/removed | Tasks 3–5 (remove HTML), menu.js stays in repo |
| Accessibility: aria-label, aria-current | Tasks 3–5 (HTML), Task 2 (JS sets aria-current) |

### Placeholder Scan

- No "TBD", "TODO", or "implement later" found.
- All code blocks contain complete, runnable code.
- All file paths are exact.
- All commands have expected outputs.

### Type Consistency

- `window.PianoApp.initNav` is defined in `js/nav.js` and called in all three HTML files.
- `aria-current="page"` is set by JS and styled by CSS consistently.
- `.page-header` class is used in CSS and all three HTML files.
- `.piano-accent` class is used in CSS, JS, and all HTML files.

No inconsistencies found.
